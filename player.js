class Player {
    constructor(id) {
        this.id = id; // Un identifiant unique pour le joueur
        this.x = 0; // Position initiale X
        this.y = 0; // Position initiale Y
        this.hp = 500;
    }

    // Vous pouvez ajouter des méthodes pour mettre à jour la position, etc.
}

module.exports = Player;
